apiVersion: v1
kind: Service
metadata:
  name: {{ template "stable01.fullname" . }}
  labels:
    app: {{ template "stable01.name" . }}
    chart: {{ template "stable01.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "stable01.name" . }}
    release: {{ .Release.Name }}
