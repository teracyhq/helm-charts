apiVersion: v1
kind: Service
metadata:
  name: {{ template "helmStable01.fullname" . }}
  labels:
    app: {{ template "helmStable01.name" . }}
    chart: {{ template "helmStable01.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helmStable01.name" . }}
    release: {{ .Release.Name }}
