apiVersion: v1
kind: Service
metadata:
  name: {{ template "helmStable02.fullname" . }}
  labels:
    app: {{ template "helmStable02.name" . }}
    chart: {{ template "helmStable02.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helmStable02.name" . }}
    release: {{ .Release.Name }}
