apiVersion: v1
kind: Service
metadata:
  name: {{ template "helmtest01.fullname" . }}
  labels:
    app: {{ template "helmtest01.name" . }}
    chart: {{ template "helmtest01.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helmtest01.name" . }}
    release: {{ .Release.Name }}
