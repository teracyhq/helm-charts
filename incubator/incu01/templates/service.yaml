apiVersion: v1
kind: Service
metadata:
  name: {{ template "incu01.fullname" . }}
  labels:
    app: {{ template "incu01.name" . }}
    chart: {{ template "incu01.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "incu01.name" . }}
    release: {{ .Release.Name }}
