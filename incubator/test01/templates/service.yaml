apiVersion: v1
kind: Service
metadata:
  name: {{ template "test01.fullname" . }}
  labels:
    app: {{ template "test01.name" . }}
    chart: {{ template "test01.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "test01.name" . }}
    release: {{ .Release.Name }}
